# 组员二前端模块说明

## 1. 责任目标

以下是“组员二”角色的建议责任边界，用于交接和答辩准备，不核实个人实际贡献。建议范围是把后端能力组织成清晰、可响应式演示的 Vue 应用，包括应用骨架、路由和会话状态、月报/流水/预算/周期/日志页面、CSV 入口、图表、表单、错误状态、桌面/手机适配以及前端自动化测试。

## 2. 负责文件与代码位置

### 2.1 前端基础设施

| 代码位置 | 关键内容 |
|---|---|
| `frontend/src/main.ts`、`App.vue` | Vue、Pinia、Router、Element Plus 初始化和全局事件 |
| `frontend/src/router/index.ts` | 月报、流水、预算、家庭、分类、周期、日志、资料以及认证/状态路由；认证/入户守卫、403 与 404 |
| `frontend/src/stores/auth.ts` | 当前用户、登录注册、认证恢复、退出 |
| `frontend/src/api/client.ts` | Axios 基地址、CSRF 初始化、401/403 分流、统一错误 |
| `frontend/src/api/*.ts` | 认证、家庭、成员、分类、流水、预算、周期、审计、统计和资料接口封装 |
| `frontend/src/types/api.ts`、`domain.ts` | 前后端 DTO 类型 |
| `frontend/src/styles/tokens.css`、`main.css` | 色彩、圆角、间距、响应式和 Element Plus 覆盖 |

### 2.2 布局、组件与页面

| 范围 | 代码位置 |
|---|---|
| 应用壳 | `frontend/src/layouts/AppLayout.vue`、`AuthLayout.vue` |
| 通用组件 | `components/PageHeader.vue`、`AmountText.vue`、`EntryForm.vue`、`StateBlock.vue`、`TrendChart.vue` |
| 认证与入户 | `views/LoginView.vue`、`RegisterView.vue`、`OnboardingView.vue` |
| 仪表盘 | `views/DashboardView.vue` |
| 业务管理 | `views/EntriesView.vue`、`MembersView.vue`、`CategoriesView.vue`、`BudgetsView.vue`、`RecurringView.vue`、`AuditLogsView.vue` |
| CSV 交互 | `components/EntryImportDialog.vue`、`api/entries.ts` 中的预览/提交/导出调用 |
| 资料与状态页 | `views/ProfileView.vue`、`ForbiddenView.vue`、`NotFoundView.vue` |
| 自动化测试 | `frontend/src/__tests__/` 下当前 12 个测试文件；具体用例数量以阶段五验证记录为准 |

## 3. 实现逻辑

### 3.1 应用启动与路由守卫

应用启动后先由 auth store 调用 `/api/auth/me` 恢复会话。路由守卫根据用户是否入户和当前角色决定去向：未登录进入登录页，已登录未入户进入 `/onboarding`，已入户进入业务页面。各页面在请求失败时按需处理 401 并回到登录流程；`client.ts` 当前只对真实 403 派发全局事件并进入 `/forbidden`；未知地址进入 404。

### 3.2 API 与 CSRF

Axios 使用 `/api` 统一前缀，Vite 开发代理转发到 Spring Boot。修改请求前确保已获取 CSRF Token，浏览器自动携带 Session Cookie；响应拦截器把后端 `{ code, message, fieldErrors }` 转换为页面可读错误。页面只传业务筛选条件，不传可信家庭 ID。

### 3.3 登录、注册与入户动线

登录/注册表单完成本地必填和格式校验后调用 auth store；成功后根据是否已有家庭跳转。入户页提供“创建家庭”和“使用邀请码加入”两个分支，成功后重新获取当前用户并进入仪表盘。登录页可识别修改密码后的查询参数，提示使用新密码。

### 3.4 仪表盘

`DashboardView.vue` 维护本月、近 3 个月、今年和自定义时间范围，把起止日期传给统计接口。接口结果映射到三张 KPI、12 月趋势、收入/支出构成、成员收入/支出/结余、上期环比、储蓄率、预算执行、异常提示和最近流水。图表在无数据时显示明确空状态；负结余使用支出语义颜色。

### 3.5 收支、成员与分类页面

收支页维护日期、类型、分类、成员和分页条件，家长能选择家庭成员，普通成员不显示越权选项。新增/编辑复用 `EntryForm.vue`，类型变化时重新筛选分类。桌面使用表格和抽屉，390px 手机宽度隐藏桌面表格并显示卡片列表与全屏表单。

成员页显示家庭通行证、家庭名称和角色；邀请码、复制和重置只对家长显示。分类页按收入/支出组织系统分类和家庭分类，普通成员只读；危险操作使用确认框。

### 3.6 预算、周期、CSV 与日志页面

预算页按月份展示总预算、分类预算和执行状态，普通成员只读，家长可保存或删除预算。周期流水页支持模板创建、编辑、启停、删除和指定月份生成；成员端只显示本人模板。流水页通过导入对话框完成 CSV 预览与提交，并提供当前筛选结果导出入口；操作日志页仅对家长显示分页时间线。

### 3.7 状态、可访问性与视觉

统一提供骨架、空数据、原位错误、重试、403 和 404。当前采用 Style C“家庭月报”视觉，颜色、圆角、阴影和间距由 token 统一；登录页使用 `frontend/public/images/login-ledger-paper-v1.png` 本地蓝纸账本背景并隐藏左上品牌标记，保留群青与朱红重点。金额同时使用文字、正负号和颜色表达，不能只依赖颜色。响应式断点保证导航、时间控件、邀请码和成员金额不产生横向滚动，并支持 `prefers-reduced-motion`。

## 4. 测试与验收

| 测试文件 | 主要验证 |
|---|---|
| `auth.spec.ts` | 认证状态恢复 |
| `client.spec.ts` | 401/403 拦截处理 |
| `router.spec.ts`、`router-guard.spec.ts` | 路由定义与守卫 |
| `entry-form.spec.ts` | 收支类型与分类联动 |
| `role-visibility.spec.ts` | 角色操作显隐 |
| `dashboard.spec.ts` | 时间范围和统计视图 |
| `date.spec.ts` | 本地日期边界格式化 |
| `entries-export.spec.ts` | CSV 导出筛选参数 |
| `household-pass.spec.ts` | 邀请码权限、复制成功/失败 |
| `profile.spec.ts` | 修改密码后退出流程 |
| `forbidden.spec.ts` | 403 页面和导航 |

建议最终执行 `npm run type-check`、`npm run test:unit`、`npm run build`，并在 1440px 与 390px 浏览器视口检查无横向溢出、主要表单可提交和角色功能显隐正确；当前实际命令、用例数量和未覆盖的 CSV 下载落盘证据见阶段五，不把定向通过写成全项目无缺陷。

## 5. 建议答辩演示

从登录页开始演示“登录—仪表盘—记一笔—筛选流水—家庭通行证—资料修改”。切换普通成员说明邀请码按钮和家长操作被隐藏，但强调真正权限仍由后端保证。最后切到 390px 展示卡片列表和控件换行，并说明路由守卫与 Axios 拦截器如何区分 401、403、404。
